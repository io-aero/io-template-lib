name: iotemplatelib
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pyyaml
  - tomli
  - tomli-w
  - virtualenv
  - pip
  - pip:
    - git+https://IoAeroMachineUser:${PYPI_PAT}@github.com/io-aero/io-common.git
